import React from 'react';
import {Pressable, Text, View} from 'react-native';
import {LAUNCHES_SCREEN} from '../../constants/routes';

export default function LaunchesScreen() {
  return (
    <View>
      <Text style={{color: 'red'}}>Launches</Text>
    </View>
  );
}
